This folder has all documentation and screenshot of features implemented

plese make a folder (inside this folder) for the feature you have implemented then add the corresponding screenshots and readme file (report or documentation) inside that folder 